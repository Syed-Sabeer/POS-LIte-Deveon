@extends('layouts.app.master')
@section('title', 'Receivables Report')
@section('content')
<div class="page-header"><div class="page-title"><h4 class="fw-bold">Receivables Report</h4><h6>Total Outstanding: PKR {{ number_format($totalDue,2) }}</h6></div></div>
<div class="card mb-3"><div class="card-body"><form method="GET" class="row g-2"><div class="col-md-4"><select name="customer_id" class="form-control"><option value="">All customers</option>@foreach($customers as $customer)<option value="{{ $customer->id }}" {{ (string)request('customer_id') === (string)$customer->id ? 'selected' : '' }}>{{ $customer->full_name }}</option>@endforeach</select></div><div class="col-md-3"><input type="date" name="from_date" value="{{ request('from_date') }}" class="form-control"></div><div class="col-md-3"><input type="date" name="to_date" value="{{ request('to_date') }}" class="form-control"></div><div class="col-md-2"><button class="btn btn-secondary w-100">Filter</button></div></form></div></div>
<div class="card"><div class="card-body p-0 table-responsive"><table class="table table-bordered mb-0"><thead class="table-light"><tr><th>Invoice</th><th>Date</th><th>Customer</th><th>Total</th><th>Paid</th><th>Due</th><th>Status</th></tr></thead><tbody>@forelse($orders as $order)<tr><td>{{ $order->order_number }}</td><td>{{ optional($order->invoice_date)->format('Y-m-d') }}</td><td>{{ $order->customer?->full_name ?: $order->customer_name }}</td><td>PKR {{ number_format($order->total,2) }}</td><td>PKR {{ number_format($order->paid_amount,2) }}</td><td>PKR {{ number_format($order->due_amount,2) }}</td><td>{{ strtoupper($order->payment_status) }}</td></tr>@empty<tr><td colspan="7" class="text-center">No outstanding receivables.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $orders->links() }}</div></div>
@endsection
